function solveFruit(fruit, weightInGrams, price) {
    let weight = weightInGrams / 1000; 
    let totalSum = weight * price;

    console.log(`I need $${totalSum.toFixed(2)} to buy ${weight.toFixed(2)} kilograms ${fruit}.`);
}

solveFruit('orange', 2500, 1.80);
solveFruit('apple', 1563, 2.35);
